name: Check with prettier

on:
  push:
  pull_request:
steps:
  - uses: actions/checkout@v2
  - uses: actions/setup-node@v2
    with:
      node-version: "14"
  - run: npm install
  - run: npm run checkPrettier
